\# Week 2, Day 1 — LLM Theory



Covered today (no coding, conceptual):

\- Tokens — text broken into chunks (\~4 chars each), pricing/limits based on tokens

\- Context window — max tokens a model can process at once

\- Embeddings — text converted to vectors capturing meaning, used for similarity search

\- How LLMs generate text — attention mechanism, one token at a time

\- Inference vs training — I'll only ever do inference

\- System vs user prompts — preview for tomorrow

